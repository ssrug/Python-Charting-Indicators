from setuptools import setup, find_packages

setup(name='Python-Charting-Indicators',
      version='0.0.1',
      description='python package to access the binance api',
      author='EasyAI, ssrug',
      url='https://github.com/ssrug/binance_api',
      packages=['technical_indicators'],
      install_requires=[
          'numpy'
        ]
      )
